/**
 * Harnais de simulation batch (CLAUDE.md — "npm run batch -- --seasons=50 --seed=X").
 * Nécessite le moteur de possession et la boucle de saison, prévus pour la
 * session suivante. Cette session (fondations P1) ne fournit que la
 * génération de ligue — voir `npm run smoke` pour l'inspecter.
 */
console.log(
  "batch/run.ts : le harnais de simulation batch n'est pas encore implémenté.\n" +
    "Il dépend du moteur de possession et de la boucle de saison (session suivante).\n" +
    "En attendant, utilisez `npm run smoke` pour inspecter une ligue générée.",
);
process.exit(1);
